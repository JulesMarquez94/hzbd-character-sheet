# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (Vite loads .env too, not only .env.local — keep keys out of git)
.env
.env.*
!.env.example

# The rulebook. Jules's own working document: it carries rulings that are still
# being argued with, and numbers marked as proposals rather than settled. It is
# not the app and it is not shipped with it, so it stays out of the repository
# until it is a thing rather than a draft.
docs/rulebook.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
